#include<stdio.h>
#include <string.h>
int main()
{
    int count,i;
    char str[40];
    printf("Enter Any Sentence=");
   gets(str);
   count=strlen(str);
   
    printf("Count Character=%d",count);
    
}
